import React, { useEffect, useRef, useState } from 'react'
import { motion, useSpring, useScroll, useTransform } from "motion/react"

const LineLoadProyect = () => {
  return (
        <motion.div style={{}} className='line'>
            <motion.div className='line-load' />
        </motion.div>
  )
}

export default LineLoadProyect